{
  "name": "app-factory-mobile",
  "version": "1.0.0",
  "description": "Autonomous App Factory — Mobile wrapper for weekly AI apps",
  "scripts": {
    "build": "echo 'Static HTML app — no build step needed'",
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap run ios",
    "cap:android": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
